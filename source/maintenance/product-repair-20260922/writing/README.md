# 写作题目入口与完整写作链路

仅维护 `product-repair-20260922/writing/`。组合接口 `integrate.py:apply(page: str) -> str`，幂等；基线正式 SHA `83346245cc01a3c48dcac31ce6c2b8a30744391cf6e4ae99f8db105103e6d166`。不改正式页、不发布；由主任务组合、验证并发布。

## 交付

- 32 道唯一完整题：20 道原有真实案例题（8 Task 1、12 Task 2）+ 已发布附件中的 9 道 Task 2 参考题 + 九分已有 3 道 Task 2。不是 32 道新题；旧工作台 6 题不换 ID。
- 搜索与 Task 筛选的平铺题目目录，每行列题名、真实 Task、已有难度、参考置信与来源。20 道旧题未有编辑难度则明确“难度未评估”。无稿先见目录；有稿先恢复当前题，固定阶段栏“换题”返回目录。
- 选题行直接到首稿，题面/图表可从阶段栏随时打开；提纲、首稿、四维检查与修订、可选再写分别显示。原有局部微练保留但不强制。
- 同题案例、局部练习、学习首答、测试链接到同一工作台稿；既有各路径字段独立原样保留。只读徽章提示已经在哪些路径留下作答，无复制或合并。
- 工作台记录返回入口；来源写入失败明确提示且不锁稿。正文保存、首稿冻结、计时和版本提交继续使用正式 `IELTSRecordStore` 与预期字段比较。
- 10 道已有阅读表达前置按原文逐字复制，保留实际材料身份、例句、回原材料链接；从阅读后写作进入时承接原入口已展开的匹配材料，不创造新匹配。
- 9 道原附件题在正文有直接原题页面与 catalog：`pr-ieltsa-writing-202609-{1..8,10}-2-reference-v{1/2}`，4 号沿用 v2，其余 v1。新的目录进度只数真实首稿、保留与修订版本。

## 映射与没有纳入的内容

完整机器映射见 `question-map.json`；搜索补充索引 `extra-index.json` 列出 32 个工作台路由与所有已识别同源路由，供主任务去重。原有 20 案例身份、来源及图表从正式正文核对，原字段详情在 `questions.json`。

- `ww-jobs` = 四行业就业；学习首答 `writing1-first`、案例 `writing1-case-wc-c21-t1-jobs`、局部 `pr-writing1-jobs`、测试 Task 1。
- `ww-housing` = 高层住宅；案例 `writing2-case-wc-c21-t1-homes`、局部 `pr-writing2-housing`、测试 Task 2。
- `ww-bricks`、`ww-cafe`、`ww-primary`、`ww-tourism` 沿用对应原题与局部练习；新的同源完整题不另建一份。
- 写作学习首答 `writing2-first` 是 C21 Test 2 剧院/影院，明确映射 `ww-wc-c21-t2-entertainment-v1`，不混入高层住宅测试。
- 教学段落、局部句子应用、技巧导读只构成原题的学习路径，不新算完整题，也不改变其作答含义。只读原料、未核题目、源图缺失题没有被包装成完整题。

## 保存与回归边界

旧 3371 字段名称、控件类型、默认值、旧选项及旧 227 catalog 单元保持。增加 688 字段：26 个新工作台题各 24 字段，32 题各增加空白阶段与返回入口字段。新编辑字段默认空，四维选择默认 `unreviewed`，后续重点默认 `task`，条件复选框默认 false；旧题原默认与历史不变。旧 picker 的选项追加且默认仍 `ww-jobs`。

写作单独组合得到 4059 字段、236 catalog 单元。健康、每日、记录并发与其余控制器逐字保持；不修改全局导航与 `learning-adjust-script`。`qa_static.py` 检查这些保护、源图存在、ID唯一、同源字段真实与前置表达逐字一致。

`qa.cjs` 使用隔离数据的真实 Edge 引擎，1440px 与 390px。覆盖32题、完整首稿/修订/四维/导出/刷新、旧六题、三路径独立历史、保存失败与返回来源失败、平铺目录搜索及真实行点击第9条近期参考题（独居）、手机一屏内可写与阶段恢复、9条直接目录路由、同源表达。

最终指纹与结果以 `static-results.json`、`qa-results.json` 为准。截图 `mobile-directory.png`、`mobile-draft.png`、`desktop-draft.png` 已查看；这是本地浏览器尺寸验证，不是手机软键盘/真机验证。公网以主任务组合后发布证据为准。
