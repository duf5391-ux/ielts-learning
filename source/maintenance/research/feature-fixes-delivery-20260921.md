# 单词、写作、口语修复与新机经

本轮接续进度审计中仍需收尾的功能、健康交互、公网加载与四端交付。没有向原任务发送暂停、改派或中断指令。全部浏览器检查使用独立上下文和合成记录，不读取用户实际学习记录。

## 正式内容

以最新正式册 `7ea735c348907a20b6b808ee403423aea263c29811071067ddca41fa85bfce44` 为基线，增量接回 `2f870d2c02b4ab48a0c64668aef59c19dbbdcb0d446df8a76a497723886b5d85`。保留原3353字段、220单元和所有旧ID；新增15个作答字段、4单元，总3368字段、224单元。

- 收藏单词表直接提供「不会／模糊／会」，无需展开释义。回忆复习也可直接标记，释义按需查看。列表自评不冒充完成复习；看过提示后的「会」不增加独立回忆次数，拼写错误仍不能记为独立拼对。
- 1000话题词卡直接提供「新学／认识少用／已会用／清除」，保留原保存字段和值。保存失败不改变原标记，也不偷偷推进复习。
- 写作首稿保留、计时和修订成功保存后才更新状态，保存失败可继续编辑；增加既有记录模型的预期字段校验，避免另一标签改动后仍锁首稿。不是localStorage跨进程事务。
- 口语修复权限请求期间重复启动、重复停止、初始化失败麦克风轨道不释放、下载扩展名与实际格式不符、录音覆盖笔记和空播放器显示。
- 从本次九分下载资料真正新增《语言起源》阅读14题和成人识字、工作生活平衡、物种灭绝3道Task 2。含作答保存、核对和对应材料词句回读。新旧15项逐项标1–5难度与依据，属于编辑评估，不是官方Band或学习者成绩。
- 阅读完整A–H与来源比对，14题独立重做与答案一致；答案是项目依据文章整理，下载资料没有官方答案键。其余下载资料保留待核，不混入已验证材料。

## 验证与维护

单词10项、写作口语11项、新机经10项真实Edge浏览器检查在同一个完整候选上通过；写作口语保留8项旧版失败对照。另补健康休息、继续、收工与刷新恢复，以及手机宽度和新增内容保存的综合检查。各项证据位于 `feature-fixes-20260921/`；公网验证另见 `release-live.json`（仅存在且pass=true时表示已通过）。

本地完整册断网查词、7组词形、20次连续查询、旧结果覆盖防护与联网句译另9项通过，证据 `feature-fixes-20260921/writing-speaking/dictionary-translation/`。英语/中文互译各用自编句向现有MyMemory服务实际请求一次，HTTP200；不是全国所有网络可用性或译文质量认证。公网词库按需联网获取，不冒称已提供完整离线缓存。

口语原生MediaRecorder使用浏览器合成麦克风生成并下载可解码音频，未声称真实麦克风、真人声音或iPhone实机验证。录音仍需单独下载，个人记录没有跨设备云同步。

维护入口 `feature-fixes-20260921/integrate_features.py`；词汇维护源 `local-dictionary/main-lookup.js`、`local-dictionary/vocabulary-review.js` 与该目录的快捷按钮脚本；写作维护源 `writing-workbench.js`，录音维护源在 `writing-speaking/speaking-recorder.js`。必须同时保留记录提交选项扩展，不只替换写作脚本。

安装清单 `feature-fixes-20260921/installation.json` 绑定正式路径、指纹、素材与备份。恢复时依照 `formal-backup/recovery.json` 逐文件还原，新增目录页可保留为未引用文件；`web-publication/extra-resources.json` 要同时恢复。跨文件安装不声称事务性。

## 发布补缺

`publish_github.ps1` 现默认带 `-Progressive`，把已验收的轻量首页和后台组装真正纳入常规同站更新。修复构建器动态加载渐进模块时未登记 `sys.modules`、在Python 3.13触发dataclass错误的问题。旧机经目录新增难度后的显式资源指纹已按通过浏览器检查的实际内容更新。

发布仅使用 `duf5391-ux/ielts-learning`，提交 `46bc80bc1d77638f019f58a2c39bfed65a99c19e` 对应Actions `35580554064` 成功，线上首页SHA `7049eb1b4979563e13dfbfc91cf4554f7c3161fee3b184f833b2e5d8613dda68` 完全匹配。公网综合9项通过、页面错误0，收工末项已闭环。

大陆同组首轮9节点首页6/9、官方音频6/9、机经音频5/9。仅失败项复测：首页1/3、官方音频2/3、机经音频2/4；上海移动三项仍连接超时，桂林首页与武汉机经音频也未通过复测。保留两轮原始结果，不合并成一次“全部通过”，不将发布成功当作大陆全网稳定可用。报告见 `research/github-publication-probes-20260921T085742.957014Z/` 与 `research/github-publication-retry-20260921T085924Z/`。

四端已生成并发布固定下载页，回下载哈希一致；具体实际产物与仍待实机项见 `research/four-platform-delivery-20260921.md`。iOS是Web Clip，桌面默认是浏览器入口；不将这些称为已实机验收的四端原生App。
