# 前端可靠性修复候选（02 / 03 / 14 / 15）

本目录仅包含候选，不曾修改正式 HTML、根组件、导航发布包或部署。正式基线 SHA-256 为 `83346245cc01a3c48dcac31ce6c2b8a30744391cf6e4ae99f8db105103e6d166`。产品依据为 Kimi 的 `research/product-pm-kimi-20260922.md`，并按主任务澄清保留真实作答语义。12 的入口与手机去重由主任务持有，本候选不修改全局导航。

组合接口：`integrate.py` 的 `apply(page: str) -> str`。主任务可先后组合写作和导航增量；若导航也改 `learning-adjust-script`，请基于本候选脚本继续局部修改，不以旧脚本覆盖。单词/复习拆分请基于本候选 `vocabulary-review-controller` 修改；整段换回 21 日版本会丢掉本轮来源显示及收藏并发保护。`learning-adjust-script.js` / `catalog-baseline.json` / 摘出的 HTML 是基线检查材料，不是组合入口。

- 02：词卡收藏写入既有词库，并同步取消；补来源与“去单词表”；保存失败双侧回退，使用旧字段值检查避免静默覆盖另一页词库。快捷自评仍不生成复习次数。
- 03：新句子保存准确材料身份、选句及来源位置，可返回并高亮所在段落，提供返回句子本链接。旧无来源记录标明“来源未记录”。保存失败保留旧收藏和窗口草稿，不显示成功。
- 14：首次口语以真实首答快照计为 0/1 → 1/1。旧保存入口不发通用提交事件，故额外在保存点击后更新进度。三个口语练习与 16 个新口语单元以已落盘文字／音频索引计算“作答已记录”，不把收藏或已读计入作答。
- 15：16 个新口语单元在题目后就地录音与可选文字草稿。全部既有录音沿用原控件身份，升级为 IndexedDB 按题逐版保存；首录和重录全部保留，有版本选择、下载、刷新恢复。失败时本次录音仅供本页回听并明确提示下载。旧文字备注不被重录文件名覆盖。

新增 32 个字段（16 × 文字草稿/音频索引），保留原 3371 字段、227 个学习单元、原 ID、每日和健康模块。音频保存在当前浏览器站点的 IndexedDB；文字 JSON 备份不包含声音，下载音频依然是跨设备备份方式。没有云同步承诺，也未做真机麦克风测试。

`qa.cjs [candidate-html]`：独立 Edge、合成数据与浏览器合成麦克风。11 项检查通过：收藏同步/回退、来源返回、旧句子、句子保存失败、首答进度/刷新、音频逐版保存/刷新/下载、权限失败、存储失败和 1440/390 视口。`qa.json` 绑定候选 SHA `134285e35366e5a326a896be520e11195b4c96bcae825f1391ef042b89b81179`。`synthetic-recording.webm` 是 3958 字节合成下载证据。早期 `qa-failure.json` 保留了已修复/测试夹具定位记录，不是当前状态。

本候选仅完成自身行为验证；主任务仍需组合导航/写作后回归真实收藏选择入口、全站路由、健康收工保护和发布包，并依据成功部署的指纹验收线上。手机截图仍有旧重复标题，这是本轮 12 的待组合部分。
