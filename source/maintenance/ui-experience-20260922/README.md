# 前端、视觉与交互升级 · 2026-09-22

用户明确继续升级前端、美工、各种交互；允许Kimi K3协助，但禁止主线程自行开子agent。主线程独自实现和验证；Kimi App K3仅做指定截图和DOM的设计评审，没有部署、修改正式文件或委派其他agent。

K3任务 `msg-9068420d-ea2b-41b8-a998-64554345c809` 已完成，`cowork_result` 返回 `executor=kimi-app, model=k3-agent`。不是Claw。原建议与模型核实分别见 `kimi-review.md`、`kimi-proposal.css`、`kimi-verification.json`。

## 本轮增量

- 自测四科卡增加独立视觉标识、实际作答进度、继续/查看结果状态和历次作答直达入口；本次刚提交的结果也立即列入历史，旧轮次仍保存在原结构。
- 阅读与写作测试：桌面题页和作答并排，手机切换看题页／写答案，字段不搬家、不复制，不清空输入；分区跳转可直接定位Part/Passage/Task。
- 写作目录改为卡片、实际草稿状态、已写过/未开始筛选与可恢复空状态；步骤条、题面与编辑区强化层级。只根据原字段展示状态，未引入AI评分或掌握度。
- 资料页修复网格选择器失配，桌面2×2；记录页把备份操作前置，增加现有单词历史、自测和稿件的快捷入口。
- 查词弹窗移除过时的“收藏才能加入”提示，记录链接指向单词历史；统一输入焦点、反馈、浮窗及移动端底栏避让。
- 背词支持键盘1/2/3；输入框、弹窗或其他页面中不触发。复习空状态显示下一次到期/去背词建议，暂停按钮不再折行。
- 既有六个一级栏目和单词内部四分区保持，定制课程仍待开放。

## 审查取舍

K3关于自测卡缺少进度/历史、资料网格选择器、复习按钮折行的建议有实际依据。写作目录原本已经会显示草稿文字状态，不能把全新空记录截图误认成“没有状态功能”；本轮加强视觉状态和筛选。原弹窗也已有样式，本轮是统一规格。未采纳删除“难度未评估”的建议，保留未知状态，不补造难度。

根线程新控件第一次探针发现旧 `.la-subject-card>a:after` 整卡点击层挡住新“开始”按钮，已移除这一处冲突；手机记录页备份前置位置也经截图重新调整。失败记录不是已发布版本。

渐进发布包第一次检查在“再做一轮”点击返回时超时，截图已呈现历史保留和新轮次。保留原报告 `packed-timeout-investigation.json`；随后同一发布包隔离性能复测完成（约2.6秒，无脚本异常），未通过扩大正式探针超时掩盖问题。以原15秒操作上限重跑完整流程，43项与19个截图状态通过。性能结果仅代表本机该次，不能由此保证所有设备响应速度。

## 维护

`build.py` 只从3ba0最新正式增量，保留4092保存字段的原顺序和属性、2470旧ID、111媒体节点。`upgrade.js` / CSS维护运行层。`verify.cjs` 在隔离浏览器中实际开始、填写、提交、查看历史、重做、刷新、写稿、备份、收藏句子、查词、键盘操作与健康休息，并截图检查双端页面。候选、发布包与实际HTTPS证据分开记录。

关系图 `interface-tensor.html` / JSON 绑定本次候选，补上自测、稿件与备份的读写关系。字段节点只是契约，图中没有用户记录。

本轮不是所有课程内容或所有真机行为审查完成。录音、软键盘、四端实机与跨设备云同步保留原边界；资料清洗和置信度状态不因视觉升级而改变。
