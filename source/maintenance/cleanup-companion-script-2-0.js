const cards=[...document.querySelectorAll('article')],skill=document.querySelector('#skill'),kind=document.querySelector('#kind'),search=document.querySelector('#search');const params=new URLSearchParams(location.search);if([...skill.options].some(o=>o.value===params.get('skill')))skill.value=params.get('skill');function filter(){let n=0;for(const c of cards){const show=(skill.value==='all'||c.dataset.skill===skill.value)&&(kind.value==='all'||c.dataset.kind===kind.value)&&c.textContent.toLowerCase().includes(search.value.trim().toLowerCase());c.hidden=!show;if(show)n++}document.querySelector('#result').textContent=n+' 份 PDF 资料'}for(const el of [skill,kind,search])el.addEventListener('input',filter);filter();