# 手机加载与独立工作台增量

当前状态：最终候选已验收并接回本地正式册；唯一公网发布任务正在同步。

## 用户范围

- 用户确认手机卡顿来自加载，改为分步后台加载；保留 GitHub Pages 网址。
- 原「工作区」入口拆分为「学习工作台」与「开发工作台」，后者有独立一级入口和大标题。两个工作台拓宽，保留学习、练习、测试及原八个学习工具去向。
- Android、Windows、iOS/iPadOS、macOS 由既有部署任务并行处理。
- 最新 IELTS Atlas 截图作为练习记录页参考：累计量、正确率、时长、连续学习、趋势和日历。没有在此版本新增统计模型或示例数字。若后续实现，应复用真实作答和时段记录，不把查看解释、一次完成等同于掌握或成绩。

## 候选与实现

正式基线 `dc3e6e39ab1bf2155069f92d6fe952733b9b044684a02d81d7f426578578938a`；合并候选 `7ea735c348907a20b6b808ee403423aea263c29811071067ddca41fa85bfce44`。

`tools/prepare_mobile_release.py` 串接媒体提取、工作台增量、运行优化，不重建整册。

- 24 个嵌入音频/图片按原字节提取，58 张图片延迟读取，32 个播放器按播放需求读取。完整 HTML 从 14,879,547 降至 3,978,782 字节。
- 输入仍同步保存；进度显示合并到下一帧；未进入的板块延后绘制；1000 词卡按 50 张建立索引。
- 公网构建调用 `tools/progressive_web.py`，先显示轻量选择页，按 396 个操作分批组装，再依原顺序运行 27 个控制器。隔离入口 138,993 字节；正式网页头、图标注入后大小以发布清单为准。
- 保留全部保存节点并恢复后才开放作答；不把此实现描述为已经可以卸载整个隐藏板块。
- 资源哈希校验，混合版本或启动失败时显示重新打开，不清空记录。

## 验证与边界

`content-pipeline/mobile-media-20260921/` 内静态检查 8/8、工作台检查 7/7、运行行为检查 11/11。`content-pipeline/progressive-boot-20260921/qa.json` 6/6，独立同伴复核通过。

完整保留 3353 个保存字段、原 ID、220 个单元、JSON 合同、每日模块边界。媒体逐字节比对通过。行为检查使用隔离合成记录，包含答题门控、首答、保存失败锁定、跨标签冲突及每日暂停恢复。

CUA 在回环地址执行真实浏览器检查：初始加载提示、题组深链、独立导航、手机宽度、测试答案刷新恢复、压缩词典查询。隔离预览最初缺少压缩词典片段而 404，补齐完整发布依赖后查询成功；不是学习内容缺失或源控制器错误。最终浏览器证据另见 `content-pipeline/mobile-media-20260921/browser-qa.json`。

桌面浏览器的手机尺寸检查不能代表用户手机硬件或每个国家网络。国内可达性与四端安装/签名情况以部署任务的实测记录为准。

## 恢复与维护

正式接回由 `tools/prepare_mobile_media.py --publish` 执行，要求候选通过检查，先备份并核对基线未变，资源先写入、主文件最后替换。2026-09-21 08:41 已接回；备份 `backups/mobile-media-20260921-084110/开始学习.html`，位置、时间及各检查绑定记入 `content-pipeline/mobile-media-20260921/publication.json`。

不要在已接回的新正式版上重新运行只用于本次基线的 `prepare_mobile_release.py`。后续增量以 README 顶部指纹为准。公网构建必须继续检查 progressive 报告中的原始资源引用及封装后的控制器语法，不能仅检查轻量首页。
