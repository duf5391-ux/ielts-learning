# 独立入口与学习链路验收

**通过；绑定最终候选 `e2b9e12e58c8f03de4b43b8ec6bdb1b81a15130ddceb6b80ea5fff6a3d742075`。** 安装器可读取同目录 `qa-final.json` 的 `pass` 和 `candidate_sha256`。

最后一版实际复验：词卡搜索已在手机首屏（y295–339），curriculum 收藏可回单词表；从词卡点击「用这个词造句」会带入选词，真正的「我的两句表达」字段 `prereq-vocab-use` 可以输入并刷新恢复。全局搜索进入新 Task 2 后回到原学习查询和单条结果；首次进入口语分类回顶部，返回恢复原位置（517→517）。每日休息、继续、收工可用，学习控制条与手机底栏不相交。

紧邻上一组合候选 `8650db…` 已完整检查 8 个桌面入口、手机 4 个直达和更多 4 项、三个合成词完整首屏、复习标记/暂停/刷新，以及每日真实填答→首答→同篇精读→休息/收工/刷新、自选恢复、测试独立答卷/提交/新轮历史、个人资料保存恢复。429 个旧地址均有目标且只显示一个主面板，这只是路由检查。

最终差异记录 `final-diff.json` 确认学习目录/路由、记录、每日、健康等脚本未变；改变的词卡入口在最终 SHA 上补验，写作改动另由写作 QA 负责。上述证据范围及继承关系均在 `qa-final.json` 明示。前一候选词卡首屏滚动失败已由最终针对性结果关闭。

这是隔离 Edge 与手机尺寸模拟验收，不是设备实测或公网验收；不覆盖逐题内容质量、音频语义，也不代替前端录音及最终发布包检查。
